# ── Required ──────────────────────────────────────────────
DISCORD_TOKEN=
CLIENT_ID=

RCON_HOST_1=
RCON_PORT_1=
RCON_PASSWORD_1=

# ── Optional — Server 2 ───────────────────────────────────
RCON_HOST_2=
RCON_PORT_2=
RCON_PASSWORD_2=

# ── Optional — feature paths & channels ──────────────────
# Directory of per-player caps ledger files (<playerId>.txt)
MODSAVE_PATH=
# Overrides the donator whitelist file location (defaults to
# <ModSave config dir>/donator.txt)
DONATOR_PATH=
MOD_LOG_CHANNEL=
LEADERBOARD_CHANNEL=

# ── Optional — logging ────────────────────────────────────
# One of: DEBUG, INFO, WARN, ERROR (default: INFO)
LOG_LEVEL=
